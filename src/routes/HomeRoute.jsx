import { Route, Routes, Navigate } from 'react-router-dom';
import Home from '../app/Home'
function HomeRouter({ children }) {
    return (
        <Navigate to="/"/>
    )
  }
  


export default HomeRouter;